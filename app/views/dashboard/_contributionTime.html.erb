<strong class='title'><%= link_to contribution.gift.title, "/gifts/#{contribution.gift.id}" %></strong>

<div class='money'>
Time:
<% timeLeft = contribution.gift.due_date - Time.now %>
<% minsLeft = timeLeft/60 %>
<% hoursLeft = minsLeft/60 %>
<% daysLeft = hoursLeft/24 %>
<% if daysLeft < 1 %>
  <% if hoursLeft < 1 %>
      <% if minsLeft > 0 %>
        <%= minsLeft.round %> minutes remaining
      <% else %>
        Contribution Period Over
      <% end %>
  <% else %>
    <%= hoursLeft.round %> hours remaining
  <% end %>
<% else %>
  <%= daysLeft.round %> days remaining
<% end %>
</div>

<div class='money'>Your Contribution: <%= number_to_currency(contribution.amount/1000) %></div>

<div class='money'>Goal: <%= number_to_currency(contribution.gift.current_total/1000, unit: "$") %>/<%= number_to_currency(contribution.gift.price/1000, unit: "$")%></div>