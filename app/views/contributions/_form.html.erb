<%= form_for(@contribution) do |f| %>
  <% if @contribution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contribution.errors.count, "error") %> prohibited this contribution from being saved:</h2>

      <ul>
      <% @contribution.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
  <div class="field small-12 columns right">
    <%= f.label :amount %>
    <%= f.text_field :amount %>
     <%= hidden_field(:contribution, :gift_id, value: @gift.id) %>
  </div>
</div>

<div class="row">
  <div class="small-12 columns right">
    <div class="actions">
      <%= f.submit %>
    </div>
  </div>
</div>


<% end %>